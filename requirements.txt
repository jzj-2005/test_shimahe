# 无人机水利巡检系统 - Python依赖包
#
# 安装方式: pip install -r requirements.txt
# 建议使用 Python 3.9 ~ 3.11
# 注意: PyTorch 由 ultralytics 自动安装，无需手动指定

# ============================================
# 视频处理
# ============================================
opencv-python>=4.8.0
ffmpeg-python>=0.2.0
#av>=11.0.0  # PyAV for RTSP streaming（可选）

# ============================================
# 深度学习 - YOLO检测
# ============================================
ultralytics>=8.3.0  # YOLOv11 需要 >=8.3.0
# torch 和 torchvision 由 ultralytics 自动安装

# ============================================
# HTTP通信
# ============================================
requests>=2.28.0  # HTTP OSD客户端数据获取

# ============================================
# 实时通信
# ============================================
paho-mqtt>=1.6.1,<2.0.0  # MQTT客户端（2.0有不兼容变更）

# ============================================
# 数据处理
# ============================================
numpy>=1.24.0,<2.0.0  # paddlepaddle 要求 numpy<2.0
pandas>=2.0.0

# ============================================
# 坐标转换
# ============================================
pyproj>=3.6.0  # WGS84 to CGCS2000 坐标转换
scipy>=1.9.0  # 旋转矩阵计算（coord_transform_new.py）

# ============================================
# 配置文件和工具
# ============================================
pyyaml>=6.0
pillow>=10.0.0  # ultralytics/paddleocr 内部依赖
tqdm>=4.65.0  # 进度条

# ============================================
# 日志
# ============================================
loguru>=0.7.0

# ============================================
# 可视化（可选）
# ============================================
matplotlib>=3.7.0  # ultralytics 训练可视化
folium>=0.14.0  # 地图可视化

# ============================================
# OCR识别
# ============================================
paddlepaddle>=2.6.0  # OCR依赖：CPU版本
paddleocr>=2.7.0
# paddlepaddle-gpu>=2.6.0  # GPU版本（如需GPU加速请注释上一行，取消注释此行）

# ============================================
# 开发和测试（可选）
# ============================================
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0  # 代码格式化
flake8>=6.1.0  # 代码检查
